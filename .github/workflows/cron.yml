name: event-agent
on:
  push:            # kör vid varje ändring på main
    branches: [ main ]
  workflow_dispatch:  # visar "Run workflow"-knappen
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow fungerar!" 
